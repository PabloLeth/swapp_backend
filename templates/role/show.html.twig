{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>RoleId</th>
                <td>{{ role.roleId }}</td>
            </tr>
            <tr>
                <th>RoleName</th>
                <td>{{ role.roleName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('role_index') }}">back to list</a>

    <a href="{{ path('role_edit', {'roleId': role.roleId}) }}">edit</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
