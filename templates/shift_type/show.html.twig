{% extends 'base.html.twig' %}

{% block title %}ShiftType{% endblock %}

{% block body %}
    <h1>ShiftType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ShiftTypeId</th>
                <td>{{ shift_type.shiftTypeId }}</td>
            </tr>
            <tr>
                <th>ShiftType</th>
                <td>{{ shift_type.shiftType }}</td>
            </tr>
            <tr>
                <th>Swappable</th>
                <td>{{ shift_type.swappable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shift_type_index') }}">back to list</a>

    <a href="{{ path('shift_type_edit', {'shiftTypeId': shift_type.shiftTypeId}) }}">edit</a>

    {{ include('shift_type/_delete_form.html.twig') }}
{% endblock %}
