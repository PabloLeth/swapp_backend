<form method="post" action="{{ path('shift_type_delete', {'shiftTypeId': shift_type.shiftTypeId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shift_type.shiftTypeId) }}">
    <button class="btn">Delete</button>
</form>
